package TestSel.saucedemo;

import java.time.Duration;
import java.util.List;

import org.junit.Assert;
import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.interactions.Actions;
import org.openqa.selenium.support.ui.ExpectedConditions;
import org.openqa.selenium.support.ui.WebDriverWait;

import io.github.bonigarcia.wdm.WebDriverManager;

public class Dub {

	public static void main(String[] args) 
	{
		/*
		 * // TODO Auto-generated method stub String productName="ZARA COAT 3";
		 * WebDriverManager.chromedriver().setup(); WebDriver driver = new
		 * ChromeDriver();
		 * driver.manage().timeouts().implicitlyWait(Duration.ofSeconds(5));
		 * //driver.get("https://rahulshettyacademy.com/client/");
		 * //driver.manage().window().maximize();
		 * 
		 * LandingPage lp = new LandingPage(driver);
		 * //driver.findElement(By.id("userEmail")).sendKeys("BB@gmail.com");
		 * //driver.findElement(By.id("userPassword")).sendKeys("Bhaskar@2");
		 * //driver.findElement(By.id("login")).click(); lp.goTO();
		 * lp.loginApplication("BB@gmail.com", "Bhaskar@2");
		 * 
		 * List<WebElement> products =
		 * driver.findElements(By.cssSelector(".col-sm-10")); WebElement prod =
		 * products.stream().filter(product->product.findElement(By.cssSelector("b")).
		 * getText().equals(productName)).findFirst().orElse(null);
		 * prod.findElement(By.cssSelector(".card-body button:last-of-type")).click();
		 * WebDriverWait wait = new WebDriverWait(driver,Duration.ofSeconds(5));
		 * wait.until(ExpectedConditions.visibilityOfElementLocated(By.cssSelector(
		 * "#toast-container")));
		 * //wait.until(ExpectedConditions.invisibilityOfElementLocated(By.cssSelector(
		 * ".ng-animating")));
		 * wait.until(ExpectedConditions.invisibilityOfElementLocated(By.cssSelector(
		 * ".ng-animating")));
		 * 
		 * driver.findElement(By.cssSelector("[routerlink*='cart']")).click();
		 * List<WebElement> cartProducts =
		 * driver.findElements(By.cssSelector(".cartSection h3"));
		 * 
		 * Boolean match =
		 * cartProducts.stream().anyMatch(cartProduct->cartProduct.getText().
		 * equalsIgnoreCase(productName)); Assert.assertTrue(true);
		 * 
		 * driver.findElement(By.cssSelector(".totalRow button")).click();
		 * 
		 * //WebElement country =
		 * Webdriver.findElement(By.cssSelector(".form-group input")).sendKeys("ind");
		 * 
		 * Actions action = new Actions(driver); action.sendKeys(driver.findElement(By.
		 * cssSelector("[placeholder*='Select Country']")), "india").build().perform();
		 * 
		 * wait.until(ExpectedConditions.visibilityOfElementLocated(By.cssSelector(
		 * ".ta-results")));
		 * driver.findElement(By.cssSelector(".ta-item:nth-of-type(2)")).click();
		 * 
		 * 
		 * 
		 * driver.findElement(By.cssSelector(".actions a")).click();
		 * 
		 * String t = driver.findElement(By.cssSelector(".hero-primary")).getText();
		 * System.out.println(t);
		 */
        		
        		
        		
        		
        
	}

}
